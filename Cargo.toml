[package]
name = "gang_simulator"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.8" # or the latest version available

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
